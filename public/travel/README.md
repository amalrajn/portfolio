Drop travel photos here.

Referenced in app/data.ts (TRAVELS[].src), e.g.:
  /travel/photo-1.jpg

Recommended: landscape ~16:9 (they render in a 2-col grid), <= 1600px wide.
Then set the matching `src`, `place`, and `caption` in app/data.ts.
